# --- core RAG stack ---
pymupdf>=1.24.0
paddleocr>=2.7.0           # OCR fallback for scanned pages
paddlepaddle>=2.6.0        # PaddleOCR runtime (heavy: ~700 MB; on Apple Silicon use 'paddlepaddle')
sentence-transformers>=3.0.0
qdrant-client>=1.10.0
httpx>=0.27.0
numpy>=1.26.0

# --- config ---
pydantic>=2.6.0
pydantic-settings>=2.3.0

# --- API layer ---
fastapi>=0.115.0
uvicorn[standard]>=0.30.0

# --- monitoring ---
prometheus-client>=0.20.0
prometheus-fastapi-instrumentator>=7.0.0

# --- scraping (download_pdfs.py) ---
requests>=2.31.0
beautifulsoup4>=4.12.0
